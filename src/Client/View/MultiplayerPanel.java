package Client.View;

import Client.Controller.PlayerController;

import javax.swing.*;

public class MultiplayerPanel
{
    public static JPanel createMultiplayerPanel(JFrame jFrame, PlayerController playerController)
    {
        JPanel panel=new JPanel();
        panel.setName("p2");

        return panel;
    }
}
